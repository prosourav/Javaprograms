package polymorphism;
//polymorphism has two types static and dynamic
// overloading is static(compile time) and overridding is dynamic(runtime)
//this is  example of runtime
public class Dog extends Pet {
 public void walk() {
	 System.out.println("Dog is walking.");
 }
}
